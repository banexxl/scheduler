# Supabase

Use three clients:
- browser
- server
- admin (server-only)

Authentication is global.

Never use the service role from client code.

Use existing RPCs:
- create_tenant()
- register_as_tenant_customer()
